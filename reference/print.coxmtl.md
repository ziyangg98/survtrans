# Print method for a `coxmtl` object

Print method for a `coxmtl` object

## Usage

``` r
# S3 method for class 'coxmtl'
print(x, ...)
```

## Arguments

- x:

  An object of class `coxmtl`.

- ...:

  Additional arguments passed to `print.summary.coxmtl`.

## Value

Invisibly returns `x`.
