# Print method for a `coxtrans` object

Print method for a `coxtrans` object

## Usage

``` r
# S3 method for class 'coxtrans'
print(x, ...)
```

## Arguments

- x:

  An object of class `coxtrans`.

- ...:

  Additional arguments passed to `print.summary.coxtrans`.

## Value

Invisibly returns `x`.
