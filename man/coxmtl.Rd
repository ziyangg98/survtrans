% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/coxmtl.R
\name{coxmtl}
\alias{coxmtl}
\title{Symmetric Multi-Task Cox Model with Global Centers}
\usage{
coxmtl(
  formula,
  data,
  group,
  w,
  lambda1 = 0,
  lambda2 = 0,
  lambda3 = 0,
  penalty = c("lasso", "MCP", "SCAD"),
  gamma = switch(penalty, SCAD = 3.7, MCP = 3, 1),
  vartheta = NULL,
  control,
  ...
)
}
\arguments{
\item{formula}{A formula with a \code{\link[survival]{Surv}} response.}

\item{data}{A data frame containing the variables in the model.}

\item{group}{A factor indicating the group of each observation.}

\item{w}{A non-negative matrix with one row per global center and one column
per group. Each row must sum to one and defines a convex combination of the
group-specific coefficients.}

\item{lambda1}{Sparse penalty applied to every group-specific coefficient.}

\item{lambda2}{Fusion penalty applied to every pairwise group difference.}

\item{lambda3}{Center penalty (scalar or vector of length \code{nrow(w)}).}

\item{penalty}{Penalty type: \code{"lasso"}, \code{"MCP"}, or
\code{"SCAD"}.}

\item{gamma}{Concavity parameter for MCP/SCAD. Default 3.7 (SCAD) or
3.0 (MCP).}

\item{vartheta}{Initial augmented Lagrangian parameter. If \code{NULL},
lasso uses 1.0 and non-convex penalties use a curvature-safe value.}

\item{control}{A \link{survtrans_control} object.}

\item{...}{Additional arguments passed to \code{survtrans_control}.}
}
\value{
An object of class \code{coxmtl}.
}
\description{
Fits a target-free multi-task Cox model across all groups using three
symmetric penalties: group-wise sparsity, pairwise fusion, and shrinkage
toward user-defined global centers.
}
