% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{penalty_value}
\alias{penalty_value}
\title{Compute penalty for a numeric vector using specified regularization type.}
\usage{
penalty_value(x, penalty, lambda, gamma)
}
\arguments{
\item{x}{A numeric vector for which the penalty is computed.}

\item{penalty}{A character string specifying the penalty type. Valid options
are "lasso", "MCP", or "SCAD".}

\item{lambda}{A numeric value representing the regularization parameter.}

\item{gamma}{A numeric value used in the penalty for MCP and SCAD.}
}
\value{
A numeric value representing the computed penalty.
}
\description{
This function computes the penalty for a given parameter vector \code{x} based on
the specified penalty approach.
}
\details{
The computation differs according to the penalty type:
\describe{
\item{lasso}{Returns the L1 norm of \code{x} scaled by \code{lambda}.}
\item{MCP}{Applies a minimax concave penalty where the penalty function
behaves linearly when the absolute values are below
\code{lambda * gamma} and quadratically otherwise.}
\item{SCAD}{Applies the smoothly clipped absolute deviation penalty;
different expressions are used when \code{abs(x)} is below
\code{lambda}, between \code{lambda} and \code{gamma * lambda}, or above
these thresholds.}
}
}
